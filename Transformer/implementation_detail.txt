[Class list]

1) Init Input Shape Example:
    [32, 50, 1024]

2) Input Embedding Vector Space(init_dim, dim):
    nn.Embedding(1024, 768)

3) Positional Encoding(Positional Embedding Vector)(max_len, dim):
    nn.Linear(max_len, 768)

=> 2) + 3): before matrix multiplication with projection matrix,
   matrix sum which is meaning that add content vector & position vector
